## Plan: Unified Simulation Task Pipeline

Standardize simulation-task mutations on an MCP-first lifecycle, treat `@agent` delegation as a pending task-offer flow, enforce task work against the simulation clock, and package completed deliverables into the final report folder. Keep REST/UI and XML as compatibility surfaces that translate into the same lifecycle instead of maintaining parallel task systems.

**Steps**
1. Phase 1 — Canonical lifecycle and persistence foundation. Extend `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/core/task_lifecycle.py` and `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/core/simulation_task_store.py` so the single task model supports `offered`, `open`, `in_progress`, `blocked`, `done`, `declined`, and `expired`; carry origin metadata, mention context, deadline fields, and artifact references in `SimulationTask` / `TaskEvent`; and add cross-process file safety for `sim_tasks.json` plus artifact writes because the API, runner, and MCP server can all touch the same simulation state. This phase blocks the rest.
2. Phase 2 — MCP-first task contract with compatibility shims. Update `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/mcp_servers/task_server.py` to expose the standard runtime tools (`offer_task`, `accept_task`, `decline_task`, `start_task`, `block_task`, `complete_task`, `list_my_tasks`, `get_task`, and an artifact-save tool), then update `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/utils/oasis_llm.py` so simulation/actor context is still auto-injected and hidden from schemas. Keep `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/core/task_action_parser.py` as a compatibility-only translator from legacy XML into the same lifecycle, and reduce prompt guidance in `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/services/oasis_profile_generator.py` so agents are pushed toward MCP instead of XML. This depends on Phase 1.
3. Phase 3 — Mention-driven offers inside the simulation loop. Extend `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/scripts/run_parallel_simulation.py` and any still-supported single-platform runners so a public `@Agent` delegation creates a pending offer by default when no structured task offer already exists in that round, but marks it as a compatibility path (`origin=mention_compat`) while MCP remains canonical. Correlate the original post/comment snippet to the task event, persist notifications instead of keeping them only in `pending_notifications`, and make offer acceptance/decline available to the receiver on the next round. This depends on Phases 1 and 2.
4. Phase 4 — Time-bounded task execution. Stamp every offer/task with `created_round`, `due_round`, and remaining-round budget using existing run-state data from `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/api/simulation.py` and the simulation runners. Enhance `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/core/task_context_injector.py` so agents first resolve pending offers, then progress accepted work, see the remaining simulation time, and are prompted to escalate blocked tasks before the end of the run. At simulation end, unfinished work should be marked `expired` with a final event. This depends on Phase 1 and can proceed in parallel with parts of Phase 5 once the task schema is stable.
5. Phase 5 — Deliverable and artifact pipeline. Introduce a simulation-scoped task-artifact staging area under the simulation uploads tree, then extend completion flow so tasks can finish with either inline text output or attached `.md`, `.txt`, and `.pdf` deliverables. During report generation in `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/tools/generate_report.py`, `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/services/report_agent.py`, and `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/resources/reports/report_store.py`, copy completed task artifacts into `uploads/reports/<report_id>/deliverables/<issue_key>/` and write a manifest that records source task, assignee, completion timing, file types, and any mapped report section. The report agent should load completed tasks and artifact metadata so final reports can cite task work directly. This depends on Phase 1 and can run in parallel with Phase 6 after the API contract is set.
6. Phase 6 — Consolidated API surface. Extend the existing task endpoints in `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/api/simulation.py` instead of creating a second API family: `GET /tasks` should return offers, deadlines, origins, artifact summaries, and richer event metadata; add action routes for `accept`, `decline`, and artifact access; and extend `get_run_status_detail()` so the frontend can request task events or a merged action-plus-task feed without polling a separate endpoint. Extend `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/api/report.py` so `/api/report/chat` optionally accepts `task_ref` / deliverable context. This depends on Phases 1 and 2, and unblocks the UI work.
7. Phase 7 — UI/UX integration across Step 3, Step 4, and Step 5. Update `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/components/Step3Simulation.vue` so the live feed merges task events with social actions and highlights `@Agent` mentions. Update `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/components/TaskPanel.vue` so users can see `Offered`, `Declined`, and `Expired` groups, accept/decline pending offers, view countdown-to-deadline information, and inspect/upload deliverables. Update `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/components/Step4Report.vue` so Step 4 shows a report-package deliverables view grouped by task and linked to the copied `.md` / `.txt` / `.pdf` outputs. Update `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/components/Step5Interaction.vue` so report-agent chat and agent interviews can optionally be scoped to a task. Use `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/api/simulation.js` and `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/api/report.js` as the client contract layer. This depends on Phase 6 but Step 3/TaskPanel work can start earlier once payload shapes are fixed.
8. Phase 8 — Migration, tests, and observability. Expand `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/tests/test_simulation_tasks.py` to cover offer/accept/decline/expire/artifact flows, XML/MCP/mention compatibility, and report packaging. Add short-run simulation smoke coverage for a full flow: mention → pending offer → accept → status updates → completion → artifact packaging. Add logs/metrics around compatibility-path usage, expired tasks, and packaging failures so you can tell when agents are still relying on XML or bare mentions. This final phase depends on Phases 1 through 7.

**Relevant files**
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/core/task_lifecycle.py` — extend `TaskLifecycleService` into the canonical offer/accept/decline/start/block/complete/expire policy layer.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/core/simulation_task_store.py` — keep `SimulationTaskStore` as the single persistence adapter; add new lifecycle states, deadline fields, artifact references, and stronger file safety.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/mcp_servers/task_server.py` — standard MCP task-tool surface for simulation agents.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/utils/oasis_llm.py` — reuse `_prepare_mcp_tool_arguments()` / `_prepare_mcp_tool_schema()` for new task tools.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/core/task_action_parser.py` — keep only as legacy XML compatibility.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/core/task_context_injector.py` — inject offers, deadlines, escalations, and active-task guidance into agent memory.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/scripts/run_parallel_simulation.py` — round-level offer creation, mention correlation, notification persistence, and expiry handling.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/api/simulation.py` — extend the current task and run-status/detail endpoints rather than create a parallel API.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/api/report.py` — add task-scoped chat and deliverable metadata to report responses.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/tools/generate_report.py` — copy completed artifacts into the report package and expose manifest data.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/backend/app/services/report_agent.py` — surface completed tasks and deliverables during report generation and Step 5 chat.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/components/Step3Simulation.vue` — merged action/task timeline and mention visibility.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/components/TaskPanel.vue` — offer acceptance, lifecycle transitions, due-time display, and deliverable UX.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/components/Step4Report.vue` — report-package deliverables UI.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/components/Step5Interaction.vue` — task-scoped chat / interview context.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/api/simulation.js` — extend existing simulation-task and status clients.
- `/Users/caravjoe/Library/CloudStorage/OneDrive-adidas/Documents/VisualStudioCode/experimental/miro-swarm/frontend/src/api/report.js` — extend report chat and deliverable retrieval clients.

**Verification**
1. Backend lifecycle tests: run `cd backend && uv run pytest tests/test_simulation_tasks.py` after extending the lifecycle, API, and compatibility coverage.
2. MCP compatibility tests: run `cd backend && uv run pytest tests/test_mcp.py tests/test_simulation_tasks.py` with the new MCP-first tool surface and XML fallback behavior.
3. Frontend build validation: run `cd frontend && npm run build` after the Step 3/4/5 changes and API-client updates.
4. Simulation smoke: run a short parallel simulation and verify the flow `@Agent` mention → pending offer → accept/decline on next round → in-progress/block/complete transitions → persisted events and deadlines in the task panel and merged feed.
5. Report smoke: generate a report from a simulation with completed tasks and verify deliverables are copied into `uploads/reports/<report_id>/deliverables/`, surfaced in Step 4, and accessible from task-scoped chat in Step 5.
6. Resume/interrupt smoke: interrupt and resume a run mid-task to verify offers, notifications, statuses, and artifact references survive process restarts.

**Decisions**
- Canonical runtime path: MCP-first for in-simulation task mutation; XML remains a compatibility shim only.
- Mention behavior: a public `@agent` delegation should create a pending task offer by default, with the receiver explicitly accepting or declining it.
- Report packaging: completed `.md`, `.txt`, and `.pdf` task outputs should be copied into the final report folder and surfaced in Step 4.
- Included scope: task lifecycle, mention-to-offer flow, time-bounded execution, report packaging, and Step 3 / Step 4 / Step 5 UX integration.
- Excluded scope: private/direct messaging between agents, replacing the OASIS runtime model, or building a separate workflow engine outside the existing simulation/report architecture.

**Further Considerations**
1. Default deadline policy: use `due_round = total_rounds` in the first pass unless a sender explicitly sets an earlier target; this keeps the rule easy for agents and the UI to explain.
2. Deliverable policy: treat inline completion summaries as valid lightweight outputs, but only stage/copy files for `.md`, `.txt`, and `.pdf` in the first pass to keep artifact handling bounded.
3. Rollout safety: keep compatibility-path usage visible in logs/metrics so you can remove legacy XML behavior once agent prompts reliably use the MCP-first tools.
